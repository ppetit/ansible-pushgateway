---
pushgateway_version: 1.0.0
pushgateway_web_listen_address: "0.0.0.0:9091"
pushgateway_web_external_url: ""

pushgateway_persistence: true

pushgateway_config_flags_extra: {}
# pushgateway_config_flags_extra:
#   persistence.interval: '5m'

prometheus_download_url: "https://github.com/prometheus/prometheus/releases/download/v"